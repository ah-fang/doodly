<section class="container">
    <h2 id="new-post-title">Create New Post</h2>
    <form class="new-post-form">
        <div class="details">
            <label for="post-title">Title <em>(required)</em></label>
            <input type="text" id="post-title" name="post-title" />
        </div>
        <div class="details">
            <label for="post-text">Description <em>(optional)</em></label>
            <input type="text" id="post-text" name="post-text" />
        </div>
        <div>
            <canvas id="canvas" width="600" height="500" style="display: block; margin: 0 auto;"></canvas>
        </div>
        <div id="post-div">
            <button id="clear" class="post-btns">clear</button>
            <button id="save" class="post-btns"> save to file</button>
            <label class="post-btns">upload file
                <input type="file" id="load" name="avatar" accept="image/png" class="pseudo-post-btn"/>
            </label>
            <button id="post" class="post-btns"> post </button>
        </div>
    </form>

</section>

{{#if posts.length}}
<section>
    <h2>Your Posts</h2>
    <ul class="homepage">
        {{#each posts as |post|}}
        <li>
            {{> dashboard-info post }}
        </li>
        {{/each}}
    </ul>
</section>
{{/if}}
